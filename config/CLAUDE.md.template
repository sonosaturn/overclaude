# Istruzioni globali

> Template OverClaude. Personalizza pure: queste regole abilitano il second-brain
> `~/brain` e l'uso di Context7. Adattale o rimuovile se non usi il vault.

**Qui vanno solo le regole valide ovunque.** Ciò che riguarda un singolo repo va nel
`CLAUDE.md` di quel repo: Claude Code carica i file nelle sottocartelle **on demand**,
quando legge file lì dentro, mentre questo lo paghi a ogni sessione di ogni progetto.
Tieni questo file sotto le ~200 righe; oltre, l'aderenza cala.

## Recall automatico delle conversazioni passate (vault ~/brain)

Esiste un second-brain personale in `~/brain` con il log curato di tutte le sessioni
in `~/brain/conversations/` (una "fotografia" markdown per sessione) e una wiki in
`~/brain/wiki/`.

**Quando l'utente fa riferimento a sessioni passate, lavoro precedente, decisioni già
prese o a un progetto per nome** (es. "come avevamo deciso…", "nella scorsa sessione",
"il progetto X", "riprendi da dove eravamo"), **prima di rispondere**:

1. Leggi `~/brain/conversations/INDEX.md` (TOC piccolo: una riga per sessione con data,
   temi, progetti). È economico, leggilo sempre per primo.
2. Individua le sessioni rilevanti e apri **solo** quei `Conv_*.md`; se serve,
   `rg -i "<termine>" ~/brain/conversations ~/brain/wiki`.
2b. **Recall semantico** (per parafrasi/sinonimi che `rg` non prende): lancia
    `~/brain/bin/brain-recall "<query>" [--conv|--kb]`. Ritorna i chunk più vicini
    per significato con score. Fail-open: se manca la key o l'indice, degrada su rg+INDEX.
    Query embeddata solo qui (mai per-prompt) → nessuno spreco.
3. Rispondi usando ciò che trovi e **cita** la sessione/pagina (es. "(sess. 25/06)").

Cerca solo quando la domanda tocca davvero lavoro passato — non ad ogni prompt. Per
domande di conoscenza generale sul vault (non sulle conversazioni) usa la skill `brain`.

## Commit automatico del vault ~/brain

`~/brain` è un repo git. **Committa automaticamente, senza chiedere conferma**, ad ogni
unità di lavoro conclusa nel vault: milestone, nuova feature, miglioramento o bugfix
(es. ingest completato, set di pagine wiki, fix di una skill/script, lint applicato).
Non a ogni turno e non a lavoro a metà — una milestone = un commit.

```
cd ~/brain && git add -A && git commit -m "<tipo>: <descrizione chiara>"
```

`<tipo>` = feat | fix | docs | refactor | chore. Solo il vault: `git add -A` dentro
`~/brain`, mai `git -C` su altri repo. Niente push automatico (resta manuale).

Stessa regola per gli altri tuoi repo personali, ma **scrivila nel `CLAUDE.md` di ciascuno**
insieme al path esatto e alle sue convenzioni (trailer AI sì/no, branch, build da verificare
prima del commit): sono dettagli che servono solo quando lavori lì.

## Skill / MCP / plugin: attivi e auto-invocabili di DEFAULT

All'installazione di una **skill**, un **MCP** o un **plugin**, configuralo perché sia
**usabile automaticamente dal modello**, senza trigger manuale — è il default voluto (context7,
ponytail, superpowers, impeccable, gitnexus, playwright, …). **Unica eccezione:** ciò che è
**user-only per design**, riconoscibile dal frontmatter `disable-model-invocation: true`
(es. `handoff`, `grill-me`) → si lancia a mano con `/<nome>`, non forzarlo auto.

- **Plugin** → abilitato in `settings.json`: `enabledPlugins: { "id@marketplace": true }`.
- **MCP** → aggiunto e `Connected` (`claude mcp add`), verificabile con `claude mcp list`.
- **Skill** → model-invocabile: **non** aggiungere `skillOverrides` che la nascondano
  (`off`/`user-invocable-only`/`name-only`) né impostare tu `disable-model-invocation`; rispetta
  invece quel flag se ce l'ha messo l'autore.
- **Verifica post-install:** skill nell'elenco del modello · MCP `Connected` · plugin `true`.

L'installer di OverClaude segue già questa logica (plugin `enabledPlugins: true`, MCP connessi,
skill non filtrate).

## Context7

Vedi `~/.claude/rules/context7.md`: usa il MCP Context7 per documentazione aggiornata di
librerie, framework, SDK, API e CLI quando l'utente fa domande a riguardo.

## Prior art

Vedi `~/.claude/rules/prior-art.md`: prima di costruire da zero una capacità non banale,
verifica se esiste già una libreria, un repo, un CLI, un MCP o una skill che la copre.

## GitNexus — code intelligence automatica (ogni repo indicizzato)

GitNexus mantiene un **grafo di conoscenza** del codice (simboli, relazioni, flussi di
esecuzione) interrogabile via tool MCP `mcp__gitnexus__*`. **Si applica a QUALSIASI repo
indicizzato** — riconoscibile dalla cartella `.gitnexus/` nella root e/o dalla presenza dei
tool `mcp__gitnexus__*`. Se il repo non è indicizzato, questa sezione non vale (e prima di
usare i tool va fatto `npx gitnexus analyze`).

**Già automatico via hook** (`~/.claude/hooks/gitnexus/gitnexus-hook.cjs`, installato in
`settings.json` da `gitnexus setup`): `PreToolUse` su Grep/Glob/Bash **arricchisce** col
contesto del grafo; `PostToolUse` su Bash **controlla la freschezza** dell'indice (avvisa dopo
`git commit`/`merge`). L'hook **non** rilancia analyze da solo → **quando avvisa che l'indice è
stale, rilancia tu** `node .gitnexus/run.cjs analyze` dalla root del repo (fallback
`npx gitnexus analyze`). Dopo un reindex i conteggi simboli/edge cambiano: è normale.

**Auto-reindex a ogni commit (repo nuovi):** l'installer registra un git `post-commit`
(`~/.local/bin/gitnexus-autoreindex.sh`) via `init.templateDir`, quindi **ogni repo creato dopo
l'install** (`git init`/`clone`) si autoimpianta GitNexus e si reindexa da solo — in background,
detached, senza bloccare git — a ogni commit, **inclusi i commit automatici** fatti seguendo le
regole di questo CLAUDE.md. Primo commit = analyze completo, poi incrementale. Repo preesistenti
non sono coperti: reindexali a mano una volta con `gitnexus analyze <path>`.

**Riflesso di base:** per capire/navigare/modificare codice, **preferisci il grafo a
grep/read**. Il grep trova stringhe; GitNexus capisce chiamanti, dipendenze e flussi.

| Quando… | Usa |
|---------|-----|
| "Come funziona X?" / esplori codice ignoto | `query({search_query:"concetto"})` (flussi raggruppati, ranked) invece di grep |
| Serve tutto il contesto di un simbolo (chiamanti, callee, flussi) | `context({name:"simbolo"})` |
| **Prima di modificare** una funzione/classe/metodo | `impact({target:"simbolo", direction:"upstream"})` → riporta blast radius e rischio |
| **Prima di committare** | `detect_changes()` (o `{scope:"compare", base_ref:"<branch-default>"}`) per verificare lo scope toccato |
| Rinominare un simbolo | `rename(...)` (capisce il call-graph) — **mai** find-and-replace |
| Tracciare un bug / "perché X fallisce?" | `trace(...)` |
| Security review (source→sink, taint) | `explain({target:"file/simbolo"})` (richiede indice con `analyze --pdg`) |
| Controllo/dato-dipendenze dentro una funzione | `pdg_query(...)` |
| Query libera sul grafo | `cypher(...)` · Mappa rotte HTTP | `route_map(...)` · Repo indicizzati | `list_repos()` |

**Regole MUST / NEVER e risorse MCP** stanno già nel `CLAUDE.md` di ogni repo indicizzato:
`gitnexus analyze` ci scrive un blocco fra i marker `<!-- gitnexus:start/end -->` con le regole
complete e le risorse `gitnexus://repo/<nome>/*` col nome giusto. Non duplicarle qui: si
caricano da sole quando lavori in quel repo. In sintesi: `impact` prima di editare (avvisa se
HIGH/CRITICAL), `detect_changes()` prima di committare, `rename` mai find-and-replace.

**Per il resto** (elenco completo dei tool, schema del grafo, workflow) usa le skill
`gitnexus-guide`, `gitnexus-exploring`, `gitnexus-debugging`, `gitnexus-impact-analysis`,
`gitnexus-refactoring`, `gitnexus-pr-review`, `gitnexus-cli`.
